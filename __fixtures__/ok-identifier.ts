/**
 * @file Fixtures - okIdentifier
 * @module fixtures/okIdentifier
 */

import type { Identifier } from 'estree'

/**
 * Node representing identifier `ok`.
 *
 * @type {Readonly<Identifier>}
 */
export default Object.freeze({ name: 'ok', type: 'Identifier' } as Identifier)
